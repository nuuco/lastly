# LASTLY 개발 이력

- 프로젝트 시작: LASTLY Sprint 1 — 로컬 인식 후 확인 저장
- Spec·골든셋 60문장 작성: 피드백 범위와 모델 결정을 문서화
- Vite + React + PWA 골격: 기록/목록 화면과 캠프용 UI
- STT: whisper-base WebGPU, processLocally Web Speech 탐침
- parse: 발화유형 규칙 게이트 + LFM2.5-350M q4 (행동·날짜)
- 저장: IndexedDB upsert, 마지막 수행일·경과일 목록, 수동 입력
- 실행: `npm install` 후 `npm run dev`. 단위 테스트는 `npm test`
- 빈 화면 수정: index.html을 직접 열면 안내 문구, 서버는 localhost:5173
- UI를 목업 구조로 교체: 목록+하단 마이크+듣기/이해/결과 시트, 인식 문장 표시
- 실시간 받아쓰기: 말하는 동안 Web Speech 중간결과를 화면에 표시, 실패 시에만 Whisper
- 인식 실패 가시화: 마이크·받아쓰기 오류를 시트에 문구로 표시, 녹음을 병행해 빈 결과면 Whisper로 재시도
- 조용한 실패 제거: 받아쓰기가 마이크를 먼저 잡게 순서 변경, processLocally 미사용, 켜짐·조각 수 진단줄과 4초 감시
- 받아쓰기 안정화: Chromium은 녹음 병행 금지(network 오류 원인), results 전체 재조립, Android 단일 result, network 3회 재시도
- 받아쓰기 재정렬: 마이크 권한 선확보 → createSpeechRecognition.start, Chrome에서 MediaRecorder 병행 금지
- 받아쓰기 검증: getUserMedia await 제거·클릭 동기 start, public/speech-check.html로 브라우저만 점검
- Chrome Whisper 폴백: Chromium은 처음부터 녹음→Whisper, Safari는 Web Speech 실시간, 2.5초 무응답 시에도 녹음 전환
- Chrome STT 진단페이지: public/speech-check.html에 onerror/onstart/onend·processLocally 로그와 에러코드 해석 표시
- 브라우저 전략 확정: 개발·실시간 STT는 Safari 기준, Chromium은 Whisper 폴백
- 목록 CRUD: 카드 탭으로 수정 시트, 이름·날짜 검증, 오늘로 기록, 삭제, updateRecord/deleteRecord
- 확인 개선: 무음 1.5초, “행동을 오늘/어제로 기록할까요?”, 말로 날짜·행동 수정 후 재확인
- STT 본선: 실시간 받아쓰기 우선, 오류·미지원 시에만 녹음→Whisper, Chrome 선제 Whisper·2.5초 강제전환 제거
- 목록 UX: 카드에 삭제 아이콘 노출, 클릭 시트는 수정·오늘 기록만
- 날짜 UI: 앱 톤 커스텀 달력(오늘/어제 칩·네이티브 date 제거)
- 삭제 확인 모달·수정 시트 상단 X 닫기
- 인식 안내: STT/parse 디버그 줄 대신 “말을 글로 받아적었어요 · …” 사용자 문구
- 조회·알림·달력: query 발화, schedule 주기, 리스트/달력 토글, 결과 TTS, 앱 오픈 due 알림
- 주기 UX: 확인·수정 칩 + “N일마다” 파싱, 되묻기에 주기 포함, 처음 켤 때 알림 권한
- 조회 UX: 매칭·답변 시트·TTS, 없으면 기록 CTA, 애매하면 후보 선택
- 달력 뷰: 마지막 수행일 점, 날짜 탭 시트, DateField 오늘/어제 칩
- 되묻기 문구 상시 표시: 듣기 중에도 “기록할까요?” 유지, TTS 타임아웃 보강
- 주기 직접: 없음/7/14/30 + 직접(일수·매주 요일·매월 마지막 요일)
- 확인 시트 X 닫기, 말한 주기→칩·되묻기 동기화·인식 패턴 보강
- 모든 시트·토스트 X 닫기, 메인/말하기에 조회 가능 안내
- 듣기 웨이브: 마이크 음량(가능 시) / Chrome은 받아쓰기 조각으로 반응
- 달력 예정(주황) 점·목록 주기 게이지·말/글 전환 아이콘 버튼
- 매월 주기: 날짜(10일)·몇째 주 요일·마지막 요일 지원
- 키보드 버튼: 하단 공간 없이 마이크 우측 구석 배치
- 마이크 힌트·키보드: 우측 여백·크기·가시성 조정
- 주기 배너 X: 우측 텍스트형, 닫으면 앱 재진입 시 재표시
- 기록 토스트: 자동 소멸만, X 제거
- 목록 날짜 헤더(오늘 · N월 N일) 제거
- 조회 안내 복구: 예문 인용 없이 ‘언제 했는지 물어볼 수 있어요’
- 바텀시트 X: 시트 안이 아니라 상단 딤드 영역에 배치
- 글 입력칸: 브랜드 초록 보더로 가시성 강화
- 바텀시트 X: 반투명·블러로 톤 다운
- 달력 셀: 숫자·점 마커 넘침 수정
- 달력 오늘: 연초록 배경 + 초록 숫자(원 없음)
- 달력 복수 표시: 1~2는 점, 3+는 숫자 뱃지
- 주기 직접: 간격(일/주/월/년) + 매주/매월 날짜·요일
- 게이지 경과(N일 지남): 위험색 표시
- 정리: dead CSS/미사용 export 제거, due 정규화 단일화, interim 표시 복구
- 리스트/달력 토글: 상단→마이크 왼쪽(상단 우측은 설정 자리)
- 하단 토글·키보드: 동일 높이·기준선 정렬
- 글 입력 시 보기 토글: 입력행 위 분리 배치
- 글 입력칸: 화면 너비 넘침 방지
- 주기 지난 배너: 문구 수정 + 눌러 목록/수정
- 주기 UI: 없음/7/14/30 + 맞춤(N·단위), 맞춤 열림 유지 수정
- 바텀시트: max-height로 X 영역 확보, 본문 스크롤







- 문서·주석 정리: 외부 프로젝트명 언급 제거, LASTLY STT 전략으로 표기
- intervalDays 제거: 저장은 schedule만, 구버전 숫자는 읽기 때 everyDays로 이관
- 파싱 전수 평가: 골든 79문장 4축(유형·행동·날짜·주기) + 접두어·조회 매칭
- guessAction 명사구: 만 절·동사 잔여 정리, 예정은 날짜 비움
- 다시 말하기: 거절 시트에서 닫지 않고 듣기 시트로 바로 전환
- 주기 맞춤 UI: 간격·요일·기준 구역 정리, 표기 다듬음
- 죽은 코드 정리: mic-dock·interval-hint CSS, extractIntervalDays, parse 이중 주기추출
- 달력 주기 점: 다음 하루만이 아니라 보이는 달의 해당일을 모두 표시
- 달력 점 위치: 날짜 숫자 바로 아래로 올림
- 슬롯 추출: 받아쓴 문장은 항상 LFM이 의도+할일·날짜·주기. 못/안만 규칙이 완료를 막음
- 완료+앞으로 주기: 했어/했고가 있으면 예정으로 덮지 않음, 이주마다는 2주
- 조회 TTS: 오늘에 했어요 중복 제거, 은/는 조사 선택
- 조회·저장 매칭: 같으면 바로, 비슷하면 이을지 묻고 별칭 저장. 청소 등 동사만 같고 대상이 다르면 다른 항목
- 조회 말투: 언제 했는지 알려 줘 인식. 거절 시트는 이유 TTS, 이대로 기록은 되묻기
